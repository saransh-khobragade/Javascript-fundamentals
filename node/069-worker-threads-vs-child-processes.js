/*
Q69. Worker threads vs child processes? When to use each?

Solution (concise):
- Worker threads: share memory (ArrayBuffer), low overhead; CPU-bound tasks.
- Child processes: separate memory/IPC; isolation; run other programs.
*/

module.exports = {};

