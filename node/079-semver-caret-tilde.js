/*
Q79. What is semantic versioning (SemVer) and how do ^ and ~ differ?

Solution (concise):
- SemVer: MAJOR.MINOR.PATCH.
- ^: allow compatible updates (changes MINOR/PATCH, same MAJOR).
- ~: allow PATCH updates only (same MAJOR and MINOR).
*/

module.exports = {};

