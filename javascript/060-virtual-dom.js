/*
Q60. How does a virtual DOM differ from the real DOM? Trade-offs?

Solution (concise):
- Virtual DOM: in-memory tree diffed to compute minimal DOM mutations.
- Pros: declarative UI, batching; Cons: overhead, not always faster for simple cases.
*/

module.exports = {};

