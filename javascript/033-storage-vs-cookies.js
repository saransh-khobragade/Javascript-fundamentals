/*
Q33. Compare localStorage, sessionStorage, and cookies.

Solution (concise):
- localStorage: persistent, per-origin, ~5–10MB, string-only, JS accessible.
- sessionStorage: per-tab session, cleared on tab close, string-only.
- cookies: sent with requests (if not HttpOnly + SameSite/Lax), small (~4KB), expires/secure flags.
*/

module.exports = {};

