/*
Q59. Difference between DOM reflow and repaint?

Solution (concise):
- Reflow: layout recalculation; expensive (affects geometry).
- Repaint: visual update without layout (color/visibility).
- Batch reads/writes and avoid forced sync layout to reduce reflows.
*/

module.exports = {};

