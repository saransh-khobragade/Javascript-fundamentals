/*
Q32. What is CORS and how is it handled on the client side?

Solution (concise):
- Browser blocks cross-origin requests unless server allows via CORS headers.
- Client cannot bypass; must rely on server headers or use a proxy.
*/

module.exports = {};

